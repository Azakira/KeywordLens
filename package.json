{
  "name": "keywordlens",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local, offline resume keyword highlighter — single self-contained HTML build.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
